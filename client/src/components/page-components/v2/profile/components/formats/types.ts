/**
 * TypeScript types for Format components
 */

export interface FormatStat {
  format: string | null;
  format_wins: number;
  format_losses: number;
  format_ties: number;
  format_played: number;
}
